# Copy to `.env` (gitignored, never committed) and fill in real values.
# Nothing here is read by the code; only `.env` is loaded.

# Required for the Phase 3C Query Planning Agent (OpenRouter provider).
OPENROUTER_API_KEY=

# Optional overrides (defaults: provider openrouter, model from config).
# CCTV_LLM_PROVIDER=openrouter
# CCTV_LLM_MODEL=meta/muse-spark-1.3
