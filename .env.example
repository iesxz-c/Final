# Copy to `.env` (gitignored, never committed) and fill in real values.
# Nothing here is read by the code; only `.env` is loaded.

# Required for the Phase 3 LLM agents (Meta Model API DIRECT).
MODEL_API_KEY=

# Optional overrides (defaults: provider meta, model muse-spark-1.3-contributor).
# CCTV_LLM_PROVIDER=meta
# CCTV_LLM_MODEL=muse-spark-1.3-contributor
